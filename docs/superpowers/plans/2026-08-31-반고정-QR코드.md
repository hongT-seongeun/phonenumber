# 반 고정 + QR 코드 다운로드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 교사 화면에서 반도 학년처럼 고정할 수 있게 하고, 생성된 링크에 대한 QR 코드를 화면에 표시하고 PNG로 다운로드할 수 있게 한다.

**Architecture:** 순수 정적 HTML/CSS/JS 3파일 구조(`index.html`/`style.css`/`script.js`)를 그대로 유지한다. 반 고정은 기존 학년 고정 패턴(select + 링크 파라미터 + 학생 화면 slot 치환)을 그대로 반복 적용한다. QR 코드는 새로 추가한 로컬 라이브러리 `qrcode.min.js`(davidshimjs/qrcodejs)로 브라우저에서 직접 `<canvas>`에 그리고, `canvas.toDataURL('image/png')`로 다운로드시킨다.

**Tech Stack:** Vanilla JS(ES5 스타일, IIFE), 순수 CSS, `qrcode.min.js`(이미 프로젝트 루트에 다운로드 완료, 신규 의존성 없음). 빌드 도구·프레임워크·테스트 러너 없음 — 이 프로젝트에는 자동화 테스트가 없으므로 각 태스크의 검증은 브라우저에서 파일을 직접 열어 확인하는 수동 테스트로 진행한다.

## Global Constraints

- ES 모듈(`type="module"`, `import`/`export`)과 로컬 `fetch`를 쓰지 않는다 — `index.html`을 더블클릭해서 바로 열 수 있어야 한다.
- QR 코드는 반드시 브라우저(클라이언트)에서 직접 생성한다 — 외부 QR 생성 API로 링크(전화번호 포함)를 보내지 않는다.
- 기존 링크 파라미터 `to`(base64url 인코딩된 전화번호), `g`(고정 학년), `c`(반 범위), `n`(번호 범위)의 이름과 동작은 바꾸지 않는다. 새 파라미터는 `fc`(고정 반) 하나만 추가한다.
- 기존 코드 스타일을 그대로 따른다: `var` 선언, 함수 선언식, 2-space 들여쓰기, 세미콜론, IIFE 구조.
- 기존 디자인 톤(칠판 카드 + 밝은 페이지 배경, `.btn-ghost`/`.result-box`/`.fixed-grade` 등 기존 클래스)을 재사용하고 불필요한 새 스타일을 만들지 않는다.

---

## Task 1: 교사 화면 — 반 고정 select 추가

**Files:**
- Modify: `index.html:25-44` (교사 화면의 학년/반/번호 설정 영역)
- Modify: `style.css` (반 범위 입력칸이 혼자 남을 때 한 칸으로 넓히는 스타일 추가)
- Modify: `script.js:34-77` (`initTeacherMode` 함수)

**Interfaces:**
- Produces: 링크 생성 시 `classLockSelect.value`(문자열, `""` 또는 `"1"`~`"15"`)를 읽어 링크에 `&fc=N`을 추가하거나(고정 시) 기존처럼 `&c=N`을 추가(비고정 시)하는 로직. 이 로직의 결과 링크 형식은 Task 2(학생 화면)가 파싱한다: 고정 시 `fc` 파라미터, 비고정 시 `c` 파라미터.

- [ ] **Step 1: `index.html`의 학년/반 설정 영역을 두 select가 나란히 있는 구조로 바꾸고, 반 범위 입력을 감싸는 wrapper에 id를 추가한다**

`index.html:25-44`의 아래 블록을:

```html
    <p class="section-tag">학생 화면에서 보여줄 범위 (선택 사항)</p>

    <label for="grade-lock">학년</label>
    <select id="grade-lock">
      <option value="">학생이 직접 선택</option>
      <option value="1">1학년으로 고정</option>
      <option value="2">2학년으로 고정</option>
      <option value="3">3학년으로 고정</option>
    </select>

    <div class="row2">
      <div>
        <label for="class-max">반은 몇 반까지</label>
        <input type="number" id="class-max" min="1" max="15" value="7" inputmode="numeric">
      </div>
      <div>
        <label for="number-max">번호는 몇 번까지</label>
        <input type="number" id="number-max" min="1" max="40" value="28" inputmode="numeric">
      </div>
    </div>
```

다음으로 교체한다:

```html
    <p class="section-tag">학생 화면에서 보여줄 범위 (선택 사항)</p>

    <div class="row2">
      <div>
        <label for="grade-lock">학년</label>
        <select id="grade-lock">
          <option value="">학생이 직접 선택</option>
          <option value="1">1학년으로 고정</option>
          <option value="2">2학년으로 고정</option>
          <option value="3">3학년으로 고정</option>
        </select>
      </div>
      <div>
        <label for="class-lock">반</label>
        <select id="class-lock">
          <option value="">학생이 직접 선택</option>
          <option value="1">1반으로 고정</option>
          <option value="2">2반으로 고정</option>
          <option value="3">3반으로 고정</option>
          <option value="4">4반으로 고정</option>
          <option value="5">5반으로 고정</option>
          <option value="6">6반으로 고정</option>
          <option value="7">7반으로 고정</option>
          <option value="8">8반으로 고정</option>
          <option value="9">9반으로 고정</option>
          <option value="10">10반으로 고정</option>
          <option value="11">11반으로 고정</option>
          <option value="12">12반으로 고정</option>
          <option value="13">13반으로 고정</option>
          <option value="14">14반으로 고정</option>
          <option value="15">15반으로 고정</option>
        </select>
      </div>
    </div>

    <div class="row2" id="range-row">
      <div id="class-max-wrap">
        <label for="class-max">반은 몇 반까지</label>
        <input type="number" id="class-max" min="1" max="15" value="7" inputmode="numeric">
      </div>
      <div>
        <label for="number-max">번호는 몇 번까지</label>
        <input type="number" id="number-max" min="1" max="40" value="28" inputmode="numeric">
      </div>
    </div>
```

- [ ] **Step 2: `style.css`에 반 범위 입력칸이 혼자 남을 때 한 칸을 다 쓰도록 하는 규칙을 추가한다**

`style.css`의 `.row2{ ... }` 규칙(93번째 줄 부근) 바로 아래에 추가:

```css
.row2.single{ grid-template-columns: 1fr; }
```

- [ ] **Step 3: `script.js`의 `initTeacherMode`에서 반 고정 select를 읽어와 반 범위 입력칸을 숨기고, 링크에 `fc`를 반영한다**

`script.js:34-77`의 `initTeacherMode` 함수 전체를 다음으로 교체한다:

```js
  function initTeacherMode() {
    var screen = document.getElementById('teacher-screen');
    screen.hidden = false;

    var phoneInput = document.getElementById('teacher-phone');
    var gradeLockSelect = document.getElementById('grade-lock');
    var classLockSelect = document.getElementById('class-lock');
    var classMaxWrap = document.getElementById('class-max-wrap');
    var rangeRow = document.getElementById('range-row');
    var classMaxInput = document.getElementById('class-max');
    var numberMaxInput = document.getElementById('number-max');
    var makeBtn = document.getElementById('make-link-btn');
    var errorEl = document.getElementById('teacher-error');
    var resultBox = document.getElementById('teacher-result');
    var resultLink = document.getElementById('result-link');
    var copyBtn = document.getElementById('copy-link-btn');
    var copyStatus = document.getElementById('copy-link-status');

    function updateClassMaxVisibility() {
      var locked = !!classLockSelect.value;
      classMaxWrap.hidden = locked;
      rangeRow.classList.toggle('single', locked);
    }
    classLockSelect.addEventListener('change', updateClassMaxVisibility);
    updateClassMaxVisibility();

    makeBtn.addEventListener('click', function () {
      var digits = onlyDigits(phoneInput.value);
      errorEl.hidden = true;
      resultBox.hidden = true;

      if (digits.length < 9) {
        errorEl.textContent = '전화번호를 다시 확인해주세요. (숫자만 9자리 이상 입력)';
        errorEl.hidden = false;
        return;
      }

      var classLock = classLockSelect.value;
      var classMax = clamp(parseInt(classMaxInput.value, 10), 1, 15, 7);
      var numberMax = clamp(parseInt(numberMaxInput.value, 10), 1, 40, 28);
      var gradeLock = gradeLockSelect.value;

      var base = window.location.origin + window.location.pathname;
      var link = base + '?to=' + encodePhone(digits);
      if (classLock) {
        link += '&fc=' + classLock;
      } else {
        link += '&c=' + classMax;
      }
      link += '&n=' + numberMax;
      if (gradeLock) {
        link += '&g=' + gradeLock;
      }
      resultLink.textContent = link;
      resultBox.hidden = false;
      copyStatus.textContent = '';

      copyBtn.onclick = function () {
        copyText(link, copyStatus);
      };
    });
  }
```

- [ ] **Step 4: 브라우저에서 열어 동작을 확인한다**

`index.html`을 더블클릭해서 연다.

1. "반" 드롭다운에서 "3반으로 고정"을 선택 → 바로 아래 "반은 몇 반까지" 입력칸이 사라지고 "번호는 몇 번까지" 칸이 한 줄을 다 차지하는지 확인.
2. 다시 "학생이 직접 선택"으로 되돌리면 "반은 몇 반까지" 칸이 다시 나타나는지 확인.
3. 전화번호에 `010-1234-5678` 입력, "반"을 "3반으로 고정"으로 둔 채 "링크 만들기" 클릭 → 결과 링크에 `&fc=3`이 있고 `&c=`는 없는지 확인.
4. "반"을 다시 "학생이 직접 선택"으로 되돌리고 "링크 만들기" 클릭 → 결과 링크에 `&c=7`(기본값)이 있고 `&fc=`는 없는지 확인.

Expected: 위 4가지 모두 설명대로 동작.

- [ ] **Step 5: 커밋**

```bash
git add index.html style.css script.js
git commit -m "교사 화면에 반 고정 select 추가"
```

---

## Task 2: 학생 화면 — 반 고정 파라미터 반영

**Files:**
- Modify: `index.html:63-70` (학생 화면의 학년/반/번호 선택 영역)
- Modify: `script.js:85-163` (`initStudentMode` 함수)

**Interfaces:**
- Consumes: Task 1이 생성하는 링크의 `fc` 파라미터(문자열 `"1"`~`"15"`, 없으면 미고정)와 기존 `c`(반 범위, 미고정일 때만 사용), `g`(고정 학년), `n`(번호 범위) 파라미터.
- Produces: 없음 (최종 사용자 화면).

- [ ] **Step 1: `index.html`의 학생 화면에서 반 select를 grade-slot과 동일한 빈 slot div로 바꾼다**

`index.html:63-70`의 아래 블록을:

```html
    <fieldset class="field-group">
      <legend>학년 / 반 / 번호</legend>
      <div class="row3">
        <div id="grade-slot"></div>
        <select id="class-select" aria-label="반 선택"></select>
        <select id="number-select" aria-label="번호 선택"></select>
      </div>
    </fieldset>
```

다음으로 교체한다:

```html
    <fieldset class="field-group">
      <legend>학년 / 반 / 번호</legend>
      <div class="row3">
        <div id="grade-slot"></div>
        <div id="class-slot"></div>
        <select id="number-select" aria-label="번호 선택"></select>
      </div>
    </fieldset>
```

- [ ] **Step 2: `script.js`의 `initStudentMode`에서 `fc` 파라미터를 읽어 반을 고정 표시하거나 select를 만든다**

`script.js:85-163`의 `initStudentMode` 함수 전체를 다음으로 교체한다:

```js
  function initStudentMode(rawTo) {
    var toDigits = onlyDigits(decodePhone(rawTo));

    if (toDigits.length < 9) {
      document.getElementById('student-error-screen').hidden = false;
      return;
    }

    var screen = document.getElementById('student-screen');
    screen.hidden = false;

    var params = new URLSearchParams(window.location.search);
    var lockedGrade = clamp(parseInt(params.get('g'), 10), 1, 3, null);
    var lockedClass = clamp(parseInt(params.get('fc'), 10), 1, 15, null);
    var classMax = clamp(parseInt(params.get('c'), 10), 1, 15, 15);
    var numberMax = clamp(parseInt(params.get('n'), 10), 1, 40, 40);

    var gradeSlot = document.getElementById('grade-slot');
    var classSlot = document.getElementById('class-slot');
    var numberSelect = document.getElementById('number-select');
    var nameInput = document.getElementById('name-input');

    var gradeSelect = null;
    if (lockedGrade) {
      var fixedGrade = document.createElement('div');
      fixedGrade.className = 'fixed-grade';
      fixedGrade.textContent = lockedGrade + '학년';
      gradeSlot.appendChild(fixedGrade);
    } else {
      gradeSelect = document.createElement('select');
      gradeSelect.setAttribute('aria-label', '학년 선택');
      gradeSlot.appendChild(gradeSelect);
      fillSelect(gradeSelect, 1, 3, '학년', '학년');
    }

    var classSelect = null;
    if (lockedClass) {
      var fixedClass = document.createElement('div');
      fixedClass.className = 'fixed-grade';
      fixedClass.textContent = lockedClass + '반';
      classSlot.appendChild(fixedClass);
    } else {
      classSelect = document.createElement('select');
      classSelect.setAttribute('aria-label', '반 선택');
      classSlot.appendChild(classSelect);
      fillSelect(classSelect, 1, classMax, '반', '반');
    }

    fillSelect(numberSelect, 1, numberMax, '번', '번호');

    var bubble = document.getElementById('preview-bubble');
    var sendBtn = document.getElementById('send-btn');

    var PLACEHOLDER_TEXT = '학년·반·번호·이름을 모두 입력하면 여기에 미리보기가 나타나요';

    function currentMessage() {
      var grade = lockedGrade || gradeSelect.value;
      var cls = lockedClass || classSelect.value;
      var num = numberSelect.value;
      var name = nameInput.value.trim();
      if (!grade || !cls || !num || !name) return null;
      return '[등록] ' + grade + '학년' + cls + '반 ' + num + '번 ' + name;
    }

    function render() {
      var msg = currentMessage();
      if (msg) {
        bubble.textContent = msg;
        bubble.classList.remove('placeholder');

        sendBtn.classList.remove('btn-disabled');
        sendBtn.setAttribute('aria-disabled', 'false');
        sendBtn.href = buildSmsLink(toDigits, msg);
      } else {
        bubble.textContent = PLACEHOLDER_TEXT;
        bubble.classList.add('placeholder');

        sendBtn.classList.add('btn-disabled');
        sendBtn.setAttribute('aria-disabled', 'true');
        sendBtn.removeAttribute('href');
      }
    }

    var watchedEls = [numberSelect];
    if (gradeSelect) watchedEls.push(gradeSelect);
    if (classSelect) watchedEls.push(classSelect);
    watchedEls.forEach(function (el) {
      el.addEventListener('change', render);
    });
    nameInput.addEventListener('input', render);

    render();
  }
```

- [ ] **Step 3: 브라우저에서 두 가지 링크로 열어 확인한다**

`index.html`을 더블클릭한 뒤 주소창 끝에 아래 두 쿼리스트링을 각각 붙여서(`file:///.../index.html?to=...`) 열어본다. (`to=MDEwMTIzNDU2Nzg`는 `01012345678`을 인코딩한 테스트용 값이다.)

**케이스 A — 반 고정:** `?to=MDEwMTIzNDU2Nzg&fc=3&n=10`
- "학년"은 드롭다운(직접 선택), "반"은 "3반"으로 고정 표시(선택 불가)되는지 확인.
- 학년 선택 + 번호 선택 + 이름 입력 시 미리보기에 `X학년3반 N번 이름`이 뜨는지 확인.

**케이스 B — 반 비고정:** `?to=MDEwMTIzNDU2Nzg&c=5&n=10`
- "반"이 1~5까지 옵션이 있는 드롭다운으로 뜨는지 확인.
- 학년 + 반 + 번호 + 이름을 모두 선택/입력해야 미리보기가 뜨는지 확인.

Expected: 두 케이스 모두 설명대로 동작.

- [ ] **Step 4: 커밋**

```bash
git add index.html script.js
git commit -m "학생 화면에서 고정된 반(fc) 파라미터 반영"
```

---

## Task 3: QR 코드 생성 + 다운로드

**Files:**
- Modify: `index.html` (head/body에 `qrcode.min.js` 스크립트 태그 추가, `#teacher-result` 안에 QR 영역 추가)
- Modify: `style.css` (QR 박스 스타일 추가)
- Modify: `script.js:34-` (`initTeacherMode`에 QR 생성/다운로드 로직 추가, 파일 하단에 `downloadQrCode` 헬퍼 추가)

**Interfaces:**
- Consumes: Task 1에서 만들어진 `link` 문자열(완성된 학생용 링크). 전역 `QRCode` 생성자(`qrcode.min.js`가 제공, `new QRCode(el, options)`, `QRCode.CorrectLevel.M`).
- Produces: 없음 (최종 사용자 화면 기능).

- [ ] **Step 1: `index.html`에 `qrcode.min.js` 스크립트 태그를 `script.js`보다 먼저 추가한다**

`index.html`의 맨 아래(90번째 줄 부근):

```html
<script src="script.js"></script>
</body>
```

를 다음으로 교체한다:

```html
<script src="qrcode.min.js"></script>
<script src="script.js"></script>
</body>
```

- [ ] **Step 2: `#teacher-result` 안에 QR 표시 영역과 다운로드 버튼을 추가한다**

`index.html`의 아래 블록을:

```html
    <div id="teacher-result" class="result-box" hidden>
      <strong>완성된 링크</strong>
      <span id="result-link"></span>
      <button type="button" id="copy-link-btn" class="btn btn-ghost">🔗 복사하기</button>
      <p id="copy-link-status" class="status-text" aria-live="polite"></p>
    </div>
```

다음으로 교체한다:

```html
    <div id="teacher-result" class="result-box" hidden>
      <strong>완성된 링크</strong>
      <span id="result-link"></span>
      <button type="button" id="copy-link-btn" class="btn btn-ghost">🔗 복사하기</button>
      <p id="copy-link-status" class="status-text" aria-live="polite"></p>
      <div id="qr-box">
        <div id="qr-canvas-wrap"></div>
        <button type="button" id="download-qr-btn" class="btn btn-ghost">⬇️ QR 다운로드</button>
      </div>
    </div>
```

- [ ] **Step 3: `style.css`에 QR 박스 스타일을 추가한다**

`style.css`의 `.result-box strong{ ... }` 규칙(184번째 줄 부근) 바로 아래에 추가:

```css
#qr-box{
  margin-top:14px;
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:10px;
}

#qr-canvas-wrap{
  background:#fff;
  padding:10px;
  border-radius:10px;
  line-height:0;
}
```

- [ ] **Step 4: `script.js`에서 링크 생성 시 QR을 그리고, 다운로드 버튼에 저장 동작을 연결한다**

`initTeacherMode` 안, `makeBtn.addEventListener('click', function () { ... })` 콜백 앞부분에 아래 두 줄을 추가한다:

```js
    var qrCanvasWrap = document.getElementById('qr-canvas-wrap');
    var downloadQrBtn = document.getElementById('download-qr-btn');
```

(다른 `var resultLink = ...` 등 선언들 바로 아래에 추가하면 된다.)

그리고 `resultBox.hidden = false;` 바로 다음 줄에 아래 코드를 추가한다:

```js
      qrCanvasWrap.innerHTML = '';
      new QRCode(qrCanvasWrap, {
        text: link,
        width: 160,
        height: 160,
        colorDark: '#000000',
        colorLight: '#ffffff',
        correctLevel: QRCode.CorrectLevel.M
      });

      downloadQrBtn.onclick = function () {
        downloadQrCode(qrCanvasWrap);
      };
```

즉 `makeBtn` 클릭 콜백은 다음과 같은 모습이 된다(전체 확인용, 변경분만 반영하면 됨):

```js
    makeBtn.addEventListener('click', function () {
      var digits = onlyDigits(phoneInput.value);
      errorEl.hidden = true;
      resultBox.hidden = true;

      if (digits.length < 9) {
        errorEl.textContent = '전화번호를 다시 확인해주세요. (숫자만 9자리 이상 입력)';
        errorEl.hidden = false;
        return;
      }

      var classLock = classLockSelect.value;
      var classMax = clamp(parseInt(classMaxInput.value, 10), 1, 15, 7);
      var numberMax = clamp(parseInt(numberMaxInput.value, 10), 1, 40, 28);
      var gradeLock = gradeLockSelect.value;

      var base = window.location.origin + window.location.pathname;
      var link = base + '?to=' + encodePhone(digits);
      if (classLock) {
        link += '&fc=' + classLock;
      } else {
        link += '&c=' + classMax;
      }
      link += '&n=' + numberMax;
      if (gradeLock) {
        link += '&g=' + gradeLock;
      }
      resultLink.textContent = link;
      resultBox.hidden = false;

      qrCanvasWrap.innerHTML = '';
      new QRCode(qrCanvasWrap, {
        text: link,
        width: 160,
        height: 160,
        colorDark: '#000000',
        colorLight: '#ffffff',
        correctLevel: QRCode.CorrectLevel.M
      });

      downloadQrBtn.onclick = function () {
        downloadQrCode(qrCanvasWrap);
      };

      copyStatus.textContent = '';

      copyBtn.onclick = function () {
        copyText(link, copyStatus);
      };
    });
```

마지막으로 `script.js` 파일 안, `copyText` 함수 바로 위(또는 아래)에 `downloadQrCode` 헬퍼 함수를 추가한다:

```js
  function downloadQrCode(canvasWrap) {
    var canvas = canvasWrap.querySelector('canvas');
    if (!canvas) return;
    var link = document.createElement('a');
    link.download = '학생등록링크_QR.png';
    link.href = canvas.toDataURL('image/png');
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  }
```

- [ ] **Step 5: 브라우저에서 열어 QR 생성과 다운로드를 확인한다**

`index.html`을 더블클릭해서 연다.

1. 전화번호 `010-1234-5678`을 입력하고 "링크 만들기" 클릭 → 링크 텍스트 아래에 흰 배경의 QR 코드 이미지가 나타나는지 확인.
2. 휴대폰 카메라(또는 QR 스캐너 앱)로 화면의 QR을 스캔 → 방금 만든 링크와 동일한 주소로 인식되는지 확인.
3. "⬇️ QR 다운로드" 버튼 클릭 → `학생등록링크_QR.png` 파일이 다운로드 폴더에 저장되는지 확인.
4. 전화번호나 옵션을 바꿔서 "링크 만들기"를 다시 클릭 → 이전 QR이 사라지고 새 QR로 교체되는지 확인(중복으로 쌓이지 않는지).

Expected: 위 4가지 모두 설명대로 동작.

- [ ] **Step 6: 커밋**

```bash
git add index.html style.css script.js
git commit -m "QR 코드 생성 및 다운로드 기능 추가"
```
